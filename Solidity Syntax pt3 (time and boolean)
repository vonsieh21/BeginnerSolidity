//import solidity
pragma solidity ^0.4.0;

contract Simple {
    // ether sazbo finney wei (we can not create variables with those name)
    bool isEqual = (1 ether == 5000 finney);
    
    //time units
    bool isTime = (1 == 1 seconds);
    
    // seconds, minutes, hours, days, weeks (dont use years)
    bool isTime = (1 hours == 60 minutes);
}
